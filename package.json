{
  "name": "wc",
  "version": "1.0.0",
  "description": "Clone of unix wc, to log the word, line, character, and byte count",
  "main": "index.js",
  "devDependencies": {
    "@types/node": "^20.11.24"
  },
  "scripts": {
    "start": "tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
